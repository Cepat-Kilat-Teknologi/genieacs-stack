# GenieACS Docker Production Environment Configuration
# ====================================================
# Copy this file to .env and configure before deployment
# cp .env.example .env
#
# IMPORTANT: Never commit .env file to version control!

# ===========================================
# REQUIRED - Security Settings
# ===========================================

# JWT Secret for UI authentication (REQUIRED - min 64 characters)
# Generate with: openssl rand -hex 32
GENIEACS_UI_JWT_SECRET=

# ===========================================
# Authentication (Keep enabled in production)
# ===========================================

# Enable authentication for NBI API
GENIEACS_NBI_AUTH=true

# Enable authentication for UI
GENIEACS_UI_AUTH=true

# ===========================================
# Network Configuration
# ===========================================

# Service ports (change if conflicts with other services)
GENIEACS_CWMP_PORT=7547
GENIEACS_NBI_PORT=7557
GENIEACS_FS_PORT=7567
GENIEACS_UI_PORT=3000

# ===========================================
# Optional: Bind to localhost only
# ===========================================
# If using reverse proxy (nginx/traefik), bind to localhost:
# GENIEACS_CWMP_PORT=127.0.0.1:7547
# GENIEACS_NBI_PORT=127.0.0.1:7557
# GENIEACS_FS_PORT=127.0.0.1:7567
# GENIEACS_UI_PORT=127.0.0.1:3000